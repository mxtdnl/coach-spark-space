# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# npm lockfile — the repo's lockfile is bun.lock; the Pages workflow runs npm install,
# not npm ci, so a local npm install must not leave a tracked lockfile behind.
package-lock.json

# test artefacts
coverage
*.junit.xml
